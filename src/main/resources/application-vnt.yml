spring:
    profiles: vnt
    application:
        name: LOCALHOSTVN

server:
    port: 8774

eureka:
    instance:
        appname: LOCALHOSTVN
    client:
        fetchRegistry: true
        serviceUrl:
            defaultZone: 'http://localhost-vn:8763/eureka/'
