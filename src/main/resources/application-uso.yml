spring:
    profiles: uso
    application:
        name: LOCALHOSTUS

server:
    port: 8771

eureka:
    instance:
        appname: LOCALHOSTUS
    client:
        fetchRegistry: true
        serviceUrl:
            defaultZone: 'http://localhost-us:8761/eureka/'
